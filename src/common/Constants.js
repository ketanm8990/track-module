import { Dimensions } from 'react-native'

const Constants = {
  Screen: {
    Login: 'Login',
  },
  Preferences: {
    User: 'user',
  },
  //font family Name
  FontFamily: {
    // QuartoRegular : 'Quarto-Regular',
  },
  //regular expressions
  Emailreg: /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{1,3})+$/,
  PasswordReg: /^[A-Za-z]+$/,
  NameReg: /^[ A-Za-z]+$/,
}

export default Constants
